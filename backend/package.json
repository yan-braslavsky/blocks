{
  "name": "@blocks/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API for Blocks MVP with mock endpoints",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "USE_MOCKS=1 vitest",
    "test:watch": "USE_MOCKS=1 vitest --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@blocks/shared": "file:../shared",
    "@fastify/cors": "^8.4.0",
    "@fastify/env": "^4.2.0",
    "fastify": "^4.24.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.3",
    "supertest": "^6.3.4",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
